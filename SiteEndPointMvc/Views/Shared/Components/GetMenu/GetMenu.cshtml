@using SadeghiBasicMarket.Application.Services.Queries.GetMenuItem;
@model List<MenuItemDto>
@{
    Layout = null;
}


@foreach (var item in Model)
{

    <li id="nav-menu-item" class="menu-item nav-overlay">
        <a href="#" class="current-link-menu">
            @item.Name
        </a>

        @if (item.Child.Count() > 0)
        {
 <ul class="sub-menu is-mega-menu mega-menu-level-two">

        @foreach (var itemChild in item.Child)
        {
             <li class="menu-mega-item menu-item-type-mega-menu">
                <a href="~/products/index?CatId=@itemChild.CatId" class="mega-menu-link">
                    @itemChild.Name
                </a>

                @*        this added
*@
                  @if (itemChild.Child.Count() > 0)
        {
            <ul class="sub-menu mega-menu-level-three">
        @foreach (var itemChildChild in itemChild.Child)
        {
            <li class="menu-mega-item-three">
                <a href="~/products/index?CatId=@itemChildChild.CatId" class="mega-menu-link">
                    @itemChildChild.Name
                </a>
                </li>
        }
        </ul>
        }
@*        this added
*@
            </li>
        }
    </ul>
        }

    </li>
}

